extends ../layout

block content
	br
	a(href="/entidades/create",class="btn btn-lg btn-success") Cadastrar Entidade
	&nbsp; &nbsp;
	a(href="/usuarios",class="btn btn-lg btn-info") Usuários
	h1 Lista de Entidades
	br
	if messages.erro
		div(class="alert alert-danger")
			p #{messages.erro}
		br
	if messages.info
		div(class="alert alert-success")
			p #{messages.info}
		br
	if lista.length > 0
		table(class="table table-striped")
			thead
				tr
					th Data:
					th Nome:
					th CNPJ:
					th Ações:
			tbody
				each valor in lista
					tr
						td #{moment(valor.data_cad).format('DD/MM/YYYY HH:MM:ss')}
						td #{valor.nomedaentidade}
						td #{valor.cnpj}
						td
							a(href="/entidades/show/#{valor._id}", class="btn btn-info")
								span(class="glyphicon glyphicon-search")
							span &nbsp;
							a(href="/entidades/edit/#{valor._id}", class="btn btn-primary")
								span(class="glyphicon glyphicon-pencil")
	else
		p Nenhum registro encontrado!
	br
	br
	a(href="/usuarios") Voltar
